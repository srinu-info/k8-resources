apiVersion: v1 
kind: Pod
metadata:
  name: pod-secrt
  labels:
    purpose: demo
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
      - secretRef:
          name: pod-secret