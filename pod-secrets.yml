appVersion: v1 
kind: Pod
metadata:
  name: nginx
  labels:
    purpose: demo
    environment: dev
spec:
  contianers:
  - name: nginx
    image: nginx
    envFrom:
      - secretRef:
          name: pod-secret