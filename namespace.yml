#login partician in kubernates cluster 
#that organise and isolate the resources, it allows mutiple teams and environments
#share saem cluster and keeping their resources separate

appVersion: app/v1
kind: namespace
metadata:
  name: namespace
  labels:
    purpose: demo
    environment: dev